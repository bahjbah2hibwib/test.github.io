export interface FormDataState {
  subject: string;
  grade: string;
  questionType: string;
  quantity: string;
  difficulty: string;
  topic: string;
  includeAnswers: boolean;
  useLatex: boolean;
  additionalRequirements: string;
  exerciseType: string;
  tone: string;
}
